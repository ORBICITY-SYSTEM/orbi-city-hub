# 📦 Node.js Installation Guide

## ⚠️ პრობლემა

**Node.js არ არის დაყენებული** - ამიტომ არ მუშაობს:
- ❌ `node` command
- ❌ `npm` command  
- ❌ `pnpm` command

---

## ✅ გადაწყვეტა: Node.js-ის დაყენება

### ნაბიჯი 1: ჩამოტვირთე Node.js

1. გადადი: **https://nodejs.org/**
2. დააჭირე **"Download Node.js (LTS)"** ღილაკს
3. ჩამოტვირთე Windows Installer (.msi ფაილი)

### ნაბიჯი 2: დააინსტალირე

1. გაუშვი ჩამოტვირთული `.msi` ფაილი
2. დააჭირე **"Next"** → **"Next"** → **"Install"**
3. ✅ დარწმუნდი რომ **"Automatically install the necessary tools"** ჩართულია
4. დააჭირე **"Finish"**

### ნაბიჯი 3: გადატვირთე Terminal

1. **დახურე** ყველა PowerShell/Terminal ფანჯარა
2. **გახსენი** ახალი Terminal
3. ან VS Code-ში: **Terminal** → **New Terminal**

### ნაბიჯი 4: შეამოწმე

```powershell
node --version
npm --version
```

უნდა გამოჩნდეს ვერსიები (მაგ: `v20.11.0` და `10.2.4`)

---

## 🚀 შემდეგი ნაბიჯები

### 1. დააინსტალირე pnpm

```powershell
npm install -g pnpm
```

### 2. გადადი პროექტის დირექტორიაში

```powershell
cd "C:\Users\tamuna.makharad_Medi\Desktop\ARCHITECTURE ORBI CITY\MTAVARI\github\program\orbi-city-hub-main\orbi-city-hub-main"
```

### 3. Install dependencies

```powershell
pnpm install
```

### 4. გაუშვი development server

```powershell
pnpm dev
```

### 5. Browser-ში გახსენი

```
http://localhost:3000/marketing/instagram
```

---

## 🔍 Alternative: Chocolatey (თუ გაქვს)

თუ Chocolatey გაქვს დაყენებული:

```powershell
choco install nodejs
```

---

**განახლებული**: 2025-01-11
