# ✅ Complete Setup Checklist - რაც საჭიროა მუშაობისთვის

## 🎯 მთავარი შედეგი: **ყველაფერი მზადაა!**

---

## ✅ რა გაქვს დაყენებული:

### 1. **Node.js** (Windows პაროლის გარეშე)
- ✅ Node.js v20.11.0 დაყენებულია (portable version)
- ✅ npm 10.2.4 მუშაობს
- ✅ pnpm 10.4.1 დაყენებულია
- 📍 მდებარეობა: `nodejs/node-v20.11.0-win-x64/`

### 2. **Dependencies**
- ✅ 1039 packages დაინსტალირებულია
- ✅ `node_modules` ფოლდერი არსებობს

### 3. **Environment Variables (.env)**
- ✅ `.env` ფაილი არსებობს და კონფიგურირებულია
- ✅ `ROWS_API_KEY` - კონფიგურირებულია
- ✅ `ROWS_SPREADSHEET_ID` - კონფიგურირებულია
- ✅ `VITE_APP_ID` - კონფიგურირებულია
- ✅ `OTELMS_USERNAME` - კონფიგურირებულია
- ✅ `OTELMS_PASSWORD` - კონფიგურირებულია
- ✅ `PORT=3000` - კონფიგურირებულია
- ✅ `NODE_ENV=development` - კონფიგურირებულია

### 4. **Database** (Optional)
- ⚠️ `DATABASE_URL` - არ არის კონფიგურირებული (არ არის აუცილებელი local dev-ისთვის)
- ℹ️ Server იმუშავებს database-ის გარეშეც, მაგრამ ზოგიერთი feature არ იმუშავებს

---

## 🚀 როგორ გავუშვა Development Server

### ნაბიჯი 1: გახსენი Terminal/PowerShell

### ნაბიჯი 2: გადადი პროექტის ფოლდერში

```powershell
cd "C:\Users\tamuna.makharad_Medi\Desktop\ARCHITECTURE ORBI CITY\MTAVARI\github\program\orbi-city-hub-main\orbi-city-hub-main"
```

### ნაბიჯი 3: დაამატე Node.js PATH-ში (ყოველ ახალ სესიაში)

```powershell
.\use-nodejs.ps1
```

**ან ხელით:**
```powershell
$env:PATH = "C:\Users\tamuna.makharad_Medi\Desktop\ARCHITECTURE ORBI CITY\MTAVARI\github\program\orbi-city-hub-main\orbi-city-hub-main\nodejs\node-v20.11.0-win-x64;$env:PATH"
```

### ნაბიჯი 4: გაუშვი Development Server

```powershell
pnpm dev
```

### ნაბიჯი 5: გახსენი Browser-ში

```
http://localhost:3000
```

**Instagram Analytics გვერდისთვის:**
```
http://localhost:3000/marketing/instagram
```

---

## 📋 რა არის საჭირო მომავალში (Optional)

### 1. **Database Connection** (თუ გინდა full functionality)
- შექმენი MySQL database
- დაამატე `.env` ფაილში:
  ```
  DATABASE_URL=mysql://user:password@localhost:3306/orbi_db
  ```
- გაუშვი migrations:
  ```powershell
  pnpm db:push
  ```

### 2. **OAuth Authentication** (თუ გინდა authentication)
- დაამატე `.env` ფაილში:
  ```
  OAUTH_SERVER_URL=https://your-oauth-server.com
  JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long
  OWNER_OPEN_ID=your-openid-for-admin-access
  ```

### 3. **Google Services** (თუ გინდა Google integrations)
- დაამატე Google API credentials `.env` ფაილში
- იხილე: `VERCEL_ENV_COMPLETE.md` სრული სიისთვის

---

## 🔧 Helper Scripts (შექმნილია შენთვის)

### 1. `use-nodejs.ps1`
**რას აკეთებს:** დაამატებს Node.js PATH-ში ამ PowerShell სესიისთვის

**როდის გამოიყენო:** ყოველ ახალ PowerShell/Terminal სესიაში, სანამ `pnpm dev` გაუშვი

### 2. `setup-nodejs.ps1`
**რას აკეთებს:** ავტომატურად ჩამოტვირთავს და დააინსტალირებს Node.js (თუ არ არსებობს)

**როდის გამოიყენო:** მხოლოდ ერთხელ, ან თუ Node.js წაშლილი გაქვს

---

## ⚠️ მნიშვნელოვანი შენიშვნები

1. **ყოველ ახალ Terminal სესიაში** უნდა გაუშვა `.\use-nodejs.ps1` ან დაამატო Node.js PATH-ში ხელით

2. **Database არ არის აუცილებელი** local development-ისთვის. Server იმუშავებს database-ის გარეშეც, მაგრამ:
   - User authentication არ იმუშავებს
   - Data persistence არ იქნება
   - ზოგიერთი feature შეიძლება არ მუშაობდეს

3. **.env ფაილი** არის `.gitignore`-ში, ასე რომ არ დაედება GitHub-ზე

4. **Development server** გაშვებულია ბექგრაუნდში. თუ გინდა რომ ნახო logs, გაუშვი ახალ terminal-ში:
   ```powershell
   pnpm dev
   ```

---

## 📞 რა ხდება თუ რამე არ მუშაობს?

### Server არ იწყება?
```powershell
# შეამოწმე Node.js:
node --version

# შეამოწმე pnpm:
pnpm --version

# შეამოწმე .env ფაილი:
Test-Path .env
```

### "Test" ღილაკი Instagram Analytics-ზე არ მუშაობს?
- შეამოწმე `.env` ფაილში `ROWS_API_KEY` და `ROWS_SPREADSHEET_ID`
- შეამოწმე browser console-ში errors (F12)
- შეამოწმე terminal-ში server logs

### Port 3000 დაკავებულია?
- Server ავტომატურად იპოვის თავისუფალ port-ს (3001, 3002, და ა.შ.)
- შეამოწმე terminal output-ში რა port-ი გამოყენებულია

---

## ✨ შედეგი

**ყველაფერი მზადაა!** შეგიძლია გაუშვა development server და დაიწყო მუშაობა!

```powershell
# ერთი ბრძანება:
.\use-nodejs.ps1 && pnpm dev
```

---

**განახლებული**: 2025-01-11
