# ============================================================================
# ORBI CITY HUB - DEMO ENVIRONMENT CONFIGURATION
# ============================================================================
# 
# Use this configuration for demo.orbicitybatumi.com deployment
# Copy these values to Vercel Environment Variables
#
# ============================================================================

# DEMO MODE FLAG - CRITICAL!
# Set to "true" to use Google Sheets instead of MySQL backend
VITE_USE_DEMO_DATA=true

# Google Apps Script Web App URL
# Replace with your deployed AppScript URL after deployment
VITE_APPSCRIPT_WEB_APP_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# Optional: Google Sheets Published CSV URLs (if using direct CSV fetch)
# VITE_SHEETS_RESERVATIONS_URL=
# VITE_SHEETS_FINANCIALS_URL=
# VITE_SHEETS_UNITS_URL=

# App Configuration
VITE_APP_TITLE=ORBI City Hub (Demo)
VITE_APP_ID=orbi-city-demo

# ============================================================================
# VERCEL DEPLOYMENT INSTRUCTIONS
# ============================================================================
#
# 1. Go to vercel.com and import the GitHub repository
# 2. In Project Settings → Environment Variables, add:
#    - VITE_USE_DEMO_DATA = true
#    - VITE_APPSCRIPT_WEB_APP_URL = [your AppScript URL]
#
# 3. Set the custom domain: demo.orbicitybatumi.com
# 4. Deploy!
#
# ============================================================================
